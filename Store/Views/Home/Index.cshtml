@model HomeViewModel



@{ ViewData["Title"] = "Home"; }


<div class="container">

    <partial name="_Carousel" />

    <h2 style="margin-top:10px">Prefered Products of the week</h2>
    <h4>Clients preferences - just for you!</h4>

    <div class="row">
    @foreach (var item in Model.PreferredProduct)
    {
    <partial name="_productSummary" model="item" />
    }
    </div>

</div>